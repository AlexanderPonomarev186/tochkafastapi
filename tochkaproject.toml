[tool.poetry]
name = "tochkaproject"
version = "0.1.0"
description = "I'm a god, hurry up with my damn message"
authors = ["Vasily Pupkin"]
license = "Ural Federation Circus"

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.80.0"
uvicorn = "^0.18.3"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
